#include <iostream>

using namespace std;

int main() {
  cout << "enter year: ";
  int y;
  cin >> y;

  int a = y % 19;
  int b = y / 100;
  int c = y % 100;
  int d = b / 4;
  int e = b % 4;
  int g = (8 * b + 13) / 25;
  int h = (19 * a + b - d - g + 15) % 30;

  return 0;
}
