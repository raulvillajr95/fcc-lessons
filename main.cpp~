


#include <iostream>
#include <vector>
using namespace std;

int main()
{
   vector<int> num = {1,2,3,4,5};

   for (int a = 1; a <= 5; a++)
   {
      num.push_back(a);
   }

   cout << "Output from begin and end: ";

   for (auto a = num.begin(); a != num.end(); ++a)
   {
      cout << *a << " ";
   }

   return 0;
}
