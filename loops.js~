/**
 *
 *
 *
 *
 *
 *
 */

function makeBackronym(str) {
  let newStr = ""
  for(let i = 0; i < str.length; i++) {
    newStr += dict[str[i].toUpperCase()]
  }
  return newStr
}
console.log(makeBackronym('dgm'))
